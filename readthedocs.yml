version: 2

sphinx:
   configuration: docs/conf.py

python:
   version: 3.7
   install:
       - requirements: docs/requirements.txt -r
   install:
       - path: myst_parser
   install:
       - path: psutil
   install:
       - path: .
