version: 2

sphinx:
   configuration: docs/conf.py

python:
   version: 3.7
   install:
       - requirements: docs/requirements.txt
   install:
       - method: pip
       - path: myst_parser
   install:
       - method: pip
       - path: .
   install:
       - method: pip
       - path: git+https://github.com/spikeinterface/spikeextractors@master